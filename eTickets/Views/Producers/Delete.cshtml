@model Producer
@{
    ViewData["Title"] = "Delete Producer";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0">
                <div class="card-header border-0 pt-4 px-4">
                    <h3 class="mb-1 text-danger">Delete Producer</h3>
                    <p class="text-muted mb-0">Are you sure you want to delete this producer?</p>
                </div>

                <div class="card-body px-4 pb-4">
                    <div asp-validation-summary="All" class="text-danger mb-3"></div>

                    <div class="d-flex gap-3 align-items-center mb-4">
                        <img src="@(string.IsNullOrEmpty(Model.ProfilePictureURL) ? Url.Content("~/images/Producers/placeholder.png") : Model.ProfilePictureURL)"
                             alt="@Model.FullName"
                             style="width:80px;height:120px;object-fit:cover;border-radius:6px;" />
                        <div>
                            <h5 class="mb-1">@Model.FullName</h5>
                            <p class="text-muted small mb-0">@Model.Bio</p>
                        </div>
                    </div>

                    <form asp-controller="Producers" asp-action="Delete" method="post"
                          onsubmit="return confirm('Are you sure you want to delete this producer? This action cannot be undone.');">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-flex justify-content-end gap-2">
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash-fill me-1"></i> Delete
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>